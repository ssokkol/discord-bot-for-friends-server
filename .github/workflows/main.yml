name: CoSec new version 

on:
  release:
    types: [published]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v1

    - name: Deploy using ssh
      uses: appleboy/ssh-action@master
      with:
        host: ${{ secrets.HOST }}
        username: ${{ secrets.USERNAME }}
        key: ${{ secrets.PRIVATE_KEY }}
        port: 22
        script: |
          cd ~/CoSecBot
          # Отменяем все локальные изменения в отслеживаемых файлах
          git reset --hard HEAD
          # Очищаем только отслеживаемые файлы, сохраняя файлы из .gitignore
          git clean -X
          # Получаем последние изменения
          git pull origin main
          # Остановка только контейнера бота
          docker-compose stop bot
          # Удаление только контейнера бота для чистой сборки
          docker-compose rm -f bot
          # Сборка только контейнера бота
          docker-compose build --no-cache bot
          # Запуск только контейнера бота
          docker-compose up -d bot
