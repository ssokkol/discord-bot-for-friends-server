# CoSecBot - Discord Bot

Современный Discord бот с использованием ООП, Docker и безопасной конфигурации. Поддерживает динамические голосовые каналы, систему значков ролей и иерархию администраторов.

## 🚀 Особенности

- **ООП архитектура** - Код организован в классы для лучшей структуры
- **Модульная структура** - Разделение на логические модули команд
- **Docker поддержка** - Готовый Docker образ для легкого развертывания
- **Безопасность** - Конфигурация через переменные окружения
- **Логирование** - Подробные логи для отладки
- **Динамические голосовые каналы** - Автоматическое создание персональных каналов
- **Система значков ролей** - Отображение значков ролей в профиле
- **Иерархия администраторов** - Многоуровневая система прав доступа

## 📁 Структура проекта

```
CoSecBot/
├── src/                          # Исходный код
│   ├── bot.py                    # Основной класс бота
│   ├── config.py                 # Конфигурация
│   ├── database.py               # Работа с базой данных
│   ├── image_generator.py        # Генерация изображений профиля
│   └── commands/                 # Модули команд
│       ├── base_command.py       # Базовый класс команд
│       ├── admin_commands.py     # Административные команды
│       ├── economy_commands.py   # Экономические команды
│       ├── top_commands.py       # Команды топов
│       ├── profile_commands.py   # Команды профиля
│       ├── global_commands.py    # Глобальные команды (/ping, /help)
│       └── voice_commands.py     # Динамические голосовые каналы
├── assets/                       # Ресурсы бота
│   ├── templates/                # Шаблоны изображений
│   ├── fonts/                    # Шрифты
│   ├── badges/                   # Значки ролей
│   └── avatars/                  # Аватары по умолчанию
├── main.py                       # Главный файл
├── init_db.py                    # Инициализация базы данных
├── requirements.txt              # Зависимости
├── Dockerfile                    # Docker образ
├── docker-compose.yml            # Docker Compose
├── run.bat                       # Скрипт запуска (Windows)
├── run.sh                        # Скрипт запуска (Linux/Mac)
├── env.example                   # Пример конфигурации
└── README.md                     # Этот файл
```

## 🛠️ Установка и запуск

### Предварительные требования

- Python 3.8+
- Docker и Docker Compose (для Docker запуска)
- Discord Bot Token

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd CoSecBot
```

### 2. Настройка конфигурации

```bash
# Копируем пример конфигурации
cp env.example .env

# Редактируем .env файл, добавляя ваш Discord токен
nano .env  # или любой другой редактор
```

**ВАЖНО:** Никогда не публикуйте файл `.env` с реальными токенами!

### 3. Перемещение ресурсов

Переместите все ресурсы в соответствующие папки:

```bash
# Создаем папки если их нет
mkdir -p assets/templates assets/fonts assets/badges assets/avatars

# Перемещаем файлы
mv *.png assets/templates/
mv *.otf assets/fonts/
mv *.jpg assets/avatars/
mv badges/*.png assets/badges/
```

### 4. Локальный запуск

```bash
# Создаем виртуальное окружение
python -m venv venv

# Активируем (Windows)
venv\Scripts\activate

# Активируем (Linux/Mac)
source venv/bin/activate

# Устанавливаем зависимости
pip install -r requirements.txt

# Запускаем бота
python main.py
```

Или используйте готовые скрипты:
- Windows: `run.bat`
- Linux/Mac: `./run.sh`

### 5. Docker запуск (рекомендуется)

```bash
# Собираем и запускаем
docker-compose up --build

# Для запуска в фоне
docker-compose up -d --build

# Просмотр логов
docker-compose logs -f discord-bot
```

## 🔧 Конфигурация

Все параметры настраиваются через файл `.env`:

```env
# ===== Основные настройки =====

# Discord Bot Token (обязательно)
DISCORD_TOKEN=your_discord_bot_token_here

# ID сервера Discord
GUILD_ID=your_guild_id_here

# ID главного администратора (имеет доступ к /give и /rem)
ADMIN_USER_ID=your_admin_user_id_here

# ===== База данных =====

# Путь к файлу базы данных
DATABASE_PATH=database.db

# ===== Активность бота =====

# Текст статуса бота
BOT_ACTIVITY_NAME=Playing

# ===== Экономика =====

# Комиссия за переводы (0.1 = 10%)
TRANSFER_COMMISSION_RATE=0.1

# Начальный баланс для новых пользователей
INITIAL_MONEY=10

# Начальное количество сообщений
INITIAL_MESSAGES=1

# ===== Роли администраторов =====

# ID ролей администраторов через запятую (по убыванию уровня доступа)
# Level 0: полный доступ (ban, kick, mute)
# Level 1: полный доступ (ban, kick, mute)
# Level 2: ограниченный доступ (kick, mute)
# Level 3: ограниченный доступ (kick, mute)
ADMIN_ROLES=role_id_0,role_id_1,role_id_2,role_id_3

# ===== Значки ролей =====

# ID ролей для отображения значков в профиле (через запятую)
BADGE_ROLES=1406251918489292963,1406252612843470969,1406255286573994026,1406283815449530408,1406283849096495266,1406284121902157854,1406284173785698334,1406284656839626872,1406284693326008373,1406284893352231072,1406284966186319923,1406285025359429825,1406285083190493294,1406690448365584425

# Специальные роли
CONTRIBUTOR_ROLE_ID=your_contributor_role_id_here
TESTER_ROLE_ID=your_tester_role_id_here
MAINTAINER_ROLE_ID=your_maintainer_role_id_here

# ===== Награды за голосовые каналы =====

# Время за минуту в голосовом канале
VOICE_TIME_REWARD=1

# Деньги за минуту в голосовом канале
VOICE_MONEY_REWARD=20

# Интервал проверки голосовых каналов (в минутах)
VOICE_CHECK_INTERVAL=1

# ===== Динамические голосовые каналы =====

# ID категории для создания динамических каналов
DYNAMIC_VOICE_CATEGORY_ID=your_category_id_here

# ID канала-лобби (при входе создается персональный канал)
DYNAMIC_VOICE_LOBBY_ID=your_lobby_channel_id_here

# ===== Отладка =====

# Уровень логирования (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Режим отладки
DEBUG_MODE=false
```

## 📊 Команды бота

### Глобальные команды (доступны везде)
- `/ping` - Проверить задержку бота

### Пользовательские команды
- `/help` - Справка по командам (показывает команды в зависимости от роли)
- `/profile` - Профиль пользователя с статистикой и значками

### Экономика
- `/transfer` - Перевод денег другому пользователю (комиссия настраивается)
- `/balance` - Проверить баланс

### Топы
- `/voice` - Топ по времени в голосовых каналах
- `/messages` - Топ по сообщениям
- `/balance` - Топ по балансу

### Административные команды

#### Полный доступ (Level 0-1)
- `/ban` - Забанить пользователя (бан вечный)
- `/kick` - Кикнуть пользователя
- `/mute` - Замутить пользователя (время в минутах, максимум 38880)

#### Ограниченный доступ (Level 2-3)
- `/kick` - Кикнуть пользователя
- `/mute` - Замутить пользователя

#### Только главный администратор
- `/give` - Выдать деньги пользователю
- `/rem` - Снять деньги у пользователя

## 🗄️ База данных

Бот автоматически создает SQLite базу данных `database.db` со следующими таблицами:

### Таблица `users`
| Поле | Тип | Описание |
|------|-----|----------|
| `id` | INTEGER | Первичный ключ |
| `user_id` | INTEGER | ID пользователя Discord |
| `messages` | INTEGER | Количество сообщений |
| `voice_time` | INTEGER | Время в голосовых каналах (в минутах) |
| `money` | INTEGER | Баланс |

## 🌟 Возможности

### Динамические голосовые каналы

Система автоматического создания персональных голосовых каналов:

1. **Вход в лобби** - При входе в канал-лобби автоматически создается персональный канал
2. **Случайные эмодзи** - Каждый канал получает уникальный эмодзи из большого списка (🎮, 🎲, 🌟, 🚀 и др.)
3. **Права владельца** - Создатель канала получает права на управление каналом и правами доступа
4. **Автоудаление** - Пустые каналы автоматически удаляются
5. **Качество звука** - Каналы создаются с битрейтом 96 kbps

### Система значков ролей

- Значки ролей отображаются в профиле пользователя
- Настраиваются через переменную `BADGE_ROLES`
- Изображения значков хранятся в `assets/badges/`

### Иерархия администраторов

Многоуровневая система прав доступа:

| Уровень | Права | Команды |
|---------|-------|---------|
| Level 0 | Полный доступ | ban, kick, mute |
| Level 1 | Полный доступ | ban, kick, mute |
| Level 2 | Ограниченный доступ | kick, mute |
| Level 3 | Ограниченный доступ | kick, mute |
| Главный админ | Максимальный доступ | give, rem + все остальные |

### Автоматическое начисление за голосовые каналы

- Каждую минуту проверяются пользователи в голосовых каналах
- Автоматическое начисление времени и денег за активность
- Не начисляется если пользователь заглушен или в AFK-канале
- Настраиваемые награды через переменные окружения

### Локализация на русский язык

- Время в голосовых каналах отображается на русском языке
- Правильное склонение для дней (день/дня/дней)
- Компактный формат времени (ч, мин, день)

## 🔒 Безопасность

- **Токены в переменных окружения** - Никогда не хардкодите токены
- **Проверка ролей** - Административные команды проверяют уровень доступа
- **Docker пользователь** - Контейнер запускается от непривилегированного пользователя
- **Валидация конфигурации** - Проверка корректности настроек при запуске

## 📝 Логирование

Логи сохраняются в:
- Консоль (при локальном запуске)
- Файл `bot.log` (при любом запуске)
- Docker контейнер (при Docker запуске)
- Монтируемый volume `./logs` (при Docker Compose)

## 🐳 Docker

### Сборка образа
```bash
docker build -t cosecbot .
```

### Запуск контейнера
```bash
docker run -d --name cosecbot --env-file .env cosecbot
```

### Просмотр логов
```bash
docker logs cosecbot
```

## 🚨 Устранение неполадок

### Ошибка "DISCORD_TOKEN не установлен"
- Проверьте файл `.env`
- Убедитесь, что токен указан правильно

### Ошибка "Шаблон не найден"
- Проверьте структуру папки `assets/`
- Убедитесь, что все файлы перемещены в правильные папки

### Ошибка "Не удалось установить русскую локаль"
- Это предупреждение, бот продолжит работать
- Для исправления установите русскую локаль в системе

### Динамические каналы не создаются
- Проверьте `DYNAMIC_VOICE_CATEGORY_ID` и `DYNAMIC_VOICE_LOBBY_ID`
- Убедитесь, что бот имеет права на создание каналов в категории

## 🤝 Вклад в проект

1. Форкните репозиторий
2. Создайте ветку для новой функции
3. Внесите изменения
4. Создайте Pull Request

## 📄 Лицензия

Этот проект распространяется под лицензией MIT.

## 🆘 Поддержка

При возникновении проблем:
1. Проверьте логи бота
2. Убедитесь, что все зависимости установлены
3. Проверьте конфигурацию в .env файле
4. Создайте Issue в репозитории

---

**CoSecBot** - Discord бот для управления сообществом
